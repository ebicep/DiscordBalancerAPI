/** Private thread / forum thread title cap (Discord). */
export const MAX_THREAD_NAME_LEN = 100;

/** Conservative cap for multi-line blocks inside messages (under Discord’s 2000 limit). */
export const MAX_MESSAGE_BLOCK_LEN = 1800;

/** Conservative cap for reply / error text (under Discord’s 2000 limit). */
export const MAX_REPLY_LENGTH = 1900;

/** Discord button label character limit. */
export const BUTTON_LABEL_MAX = 78;
